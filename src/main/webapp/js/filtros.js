function toggleEstado(checkbox) {
    // Aquí puedes agregar la lógica que deseas para manejar el cambio de estado
    // Por ejemplo, puedes imprimir en la consola el nombre del checkbox y su estado
    console.log("Checkbox name: " + checkbox.name + ", Checked: " + checkbox.checked);
}